<!-- res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme - MUST extend MaterialComponents -->
    <style name="Theme.NotesApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/accent_purple</item>
        <item name="colorPrimaryVariant">#6D28D9</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent_pink</item>
        <item name="colorSecondaryVariant">#D946EF</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar and navigation bar colors -->
        <item name="android:statusBarColor">@color/bg_gradient_start</item>
        <item name="android:navigationBarColor">@color/bg_gradient_start</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Error color -->
        <item name="colorError">@color/delete_red</item>
        <item name="colorOnError">@color/white</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/bg_gradient_start</item>
        <item name="colorSurface">@color/glass_white</item>
        <item name="colorOnSurface">@color/text_white</item>
    </style>
</resources>